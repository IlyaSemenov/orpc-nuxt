# trpc-vue Agent Guide

- Compare tRPC utility behavior against the pinned dev-only upstream version; keep React out of published runtime code and declarations.
- Build tRPC query and mutation keys only through `client/keys.ts`.
