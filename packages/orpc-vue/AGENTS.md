# orpc-vue Agent Guide

- Build oRPC query and mutation options with its official utilities, preserving their keys and client context.
- Hash oRPC keys with `hashORPCKey` both in the module's QueryClient config and in the client's namespace registration.
